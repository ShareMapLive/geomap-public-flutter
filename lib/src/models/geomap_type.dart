/// Enum representing the different types of maps supported
enum GeoMapType {
  /// Google Maps implementation (typically for mobile)
  googleMap,

  /// Flutter Map implementation (typically for web)
  flutterMap,
}
